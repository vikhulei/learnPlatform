import styled from "styled-components"

const BodyWrapper = styled.div`
    position: relative;
`

const MainWrapper = styled.div`
    position: relative;
    background-color: #f6f0ed;
    height: 100vh;
    width: 100vw;
    padding: 30px;
`

export {BodyWrapper, MainWrapper}