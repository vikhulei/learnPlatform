import {Frame, Heading} from "./CalculatorStyle"

const Calculator = () => {
    return (
        <Frame>
            <Heading>Calculator here</Heading>
        </Frame>
    )
} 

export default Calculator